services:
  db:
    image: postgres:16-alpine
    container_name: fakturcho-eu-db
    environment:
      POSTGRES_USER: fakturcho
      POSTGRES_PASSWORD: fakturcho
      POSTGRES_DB: fakturcho
    ports:
      - '54130:5432'
    volumes:
      - fakturcho-eu-db-data:/var/lib/postgresql/data

volumes:
  fakturcho-eu-db-data:
